# 项目完整待办清单（2026-08-31）

> 合并四个来源：① `docs/技术优化调研-2026-08-31.md`（7 件优先事项 + P0/P1/P2 路线图）；
> ② `docs/审计-2026-08-30.md` 未闭环；③ `docs/后续计划-2026-08-29.md`；④ 本轮会话（2026-08-31）
> 明确的可选后续。状态以当前代码为准；验证口径沿用 bakeoff 文化（先量化、无显著收益即停）。

## 0. 已闭环（2026-08-31，防止误排）

- P0-A 措辞软化（buildPlannerSpec + buildTaskPrompt 脚手架）——bakeoff 确认无回归、无显著收益
- P0-B Mission 宗旨（tenets）：核心 + 三面透传 + Web UI（设置页/launch/上下文查看器标记）
- P1 feedback 环：语义类（能力缺口）→ 执行侧约束写回 spec（v1 确定性；v2 见 §4）
- P1 team 记忆：`team:<mission_id>` 归属 + commander 收口 + Web 写入/筛选
- Web UI 三批：tenets 入口、产物查看面、看板重规划、记忆写入/owner 筛选
- **N2 记忆运行时注入（调研 §0 第 5 项 / 路线图 P1-4 的基础接线）**：派发按槽位/团队注入相关记忆
- **P0-5 planDelivery 接线**：reassign 交接按 2×3 矩阵投递 + dispatcher owner 偏好送到目标槽位
- **cron 编辑 UI**：CronPanel JSON 编辑 + cronSave + /cron POST
- Web UI 其他旧项：暂停/恢复、任务换人、rules 管理、记忆三件套 + Cron 列表面板
- **P0-2 静态前缀工程**：buildTaskPromptSegments 拆分 + ledger cache 列（详见 §1 勾选条目）
## 0.1 溯源索引：微信文章《从 ReAct 到 Agent Teams》→ 已做/未做

| 文章机制/原则 | 对应落地 | 状态 |
|---|---|---|
| 启发式管理（命令式措辞→保守分布） | P0-A 措辞软化（buildPlannerSpec/buildTaskPrompt）+ bakeoff | ✅ 无回归无显著收益，保留现状 |
| Mission/宗旨（do/prioritize 价值观锚点） | P0-B Mission.tenets + 派发注入 + Web UI | ✅ |
| 讨论→共识→执行（轻量版） | P1 feedback 环：语义类写回执行侧约束 / 结构类直重试 | ✅ v1；v2（LLM worker 咨询）在 §4 |
| 集体复盘（经验升团队资产） | P1 team 记忆 `team:<mission_id>` + commander 收口 | ✅ |
| 「上下文即记忆」 | memory 子系统 + N2 运行时注入（塞回 worker context） | ✅ |
| 消融警句（协作机制=价值来源） | 不堆 agent；Worker P2P 暂缓、预算投向质量门/记忆/验证 | ✅ 决策 |
| Leader 能力差 | commander 是宿主 agent（部分具备）；preset 机制 | ⬜ 暂缓（现成底座） |
| Worker 横向通信（P2P） | 受控结构化信息流（review 宿主注入 diff）已打磨 | ⬜ 暂缓（沙箱+context 爆炸） |
| OKR 进度 | ledger + dispatcher slotSuccess 现成底座 | ⬜ 暂缓（增量非新架构） |

---

## 1. P0 —— 每轮/每次重试都在交的固定税（改动局部、无模型依赖、收益确定）

- [x] **P0-1 工具定义分层加载（可验证核心已完成，2026-08-31）**
  - 已做：`src/core/tool-stages.ts`（stage 清单 + `presentTools` 分层呈现 + 索引行 + token 估算）；
    `pod_expand_tool` 元工具（随 bundle 自动注册到 commander，MCP server 同步注册）；
    MCP 暴露一句话 brief 表。实测：全量 15 工具 ≈2,838 tok → 分层（stage=dispatch）≈1,320 tok **−56%**
    （`scripts/measure-tools.mjs` 可复现；单测 `tests/tool-stages.test.ts`）。
  - 剩余（宿主依赖/排期项）：① commander 每轮真正按 stage 注入完整 schema——需 DSH 动态工具呈现
    （`restrict`/`tools/change`），本仓库无法直接验证，标记为宿主侧接线；② MCP `tools/list --short`
    完整协商属 P2-1（ADOL），已在 §3。
  - 验证：分层 vs 全量 token A/B（measure-tools）；工具误选率不升、成功率不回归（bakeoff 文化）。
- [x] **P0-2 静态前缀工程 + prompt cache 对齐**（调研 §1.2，2026-08-31）
  - 已做：`buildTaskPrompt` → `buildTaskPromptSegments` 拆成 `{static, dynamic}`，静态脚手架
    （charter/fallback + 交付纪律 + 报告 schema）连续前置且 **byte-稳定**（交付纪律不再内联任务 id，
    commit 前缀由 worker 从任务头解析）；动态段（任务头/工作目录/审查块/简报/交接）置尾。
  - 加前缀 hash 快照测试（同任务两次 build 前缀 hash 相等 / 同类型不同任务静态前缀 hash 相等 /
    spec 变更仅 dynamic 变）+ ledger 增加 `cache_read_tokens`/`cache_creation_tokens` 列
    （recordUsage 可选参数 + summary 汇总 + claude extractUsage 读 `cache_read_input_tokens`/
    `cache_creation_input_tokens`，W1 实证字段名）。
  - 量化（`scripts/measure-prompt-prefix.mjs`）：静态前缀跨同类型任务唯一数=1（完全 byte-稳定），
    静态 ~397 tok / 完整 ~419 tok（占 ~95%）。cache 命中率用 ledger 实测列在真实 mission 观测。
  - 落点：`claude-headless.ts`、`ledger.ts`、`types.ts`、`orchestrator.ts`；bakeoff 对照复用 wording-eval 流程。
  - 依据：Anthropic 缓存输入 10% 计费、≤4 断点；重试 3 次付 3 倍是本项目实测痛点。

## 2. P1 —— 上下文生命周期（调研 §4 P1 系列 + 审计未闭环）

- [x] **P1-1 重置摘要 ACE 化：增量 delta 账本**（调研 §2.1，2026-08-31）
  - 已做：`ResetEntry` 条目类型 + store 持久化（JSON/SQLite 双后端 `reset_entries` 集合）+ `src/core/reset-ledger.ts`（Generator=任务完成入账 Rich fact / Curator=幂等去重+重做 supersede / Reflector=只渲染 active）。`orchestrator.ts` 任务 done 后 `recordTaskFact` 入账；档位 C 重置改从账本渲染（`resetSummaryFromStore`），不再整份重写。
  - 量化（`scripts/measure-reset-ledger.mjs`）：delta 成本 ≈ 旧整写 1.10×（中性，因条目更 Rich），但信息保留 **2/5 → 5/5 字段**（测试证据/决策不再丢失）——ACE 主张的质量收益实锤；成本中性 + 质量提升 + P1-2/3/4 底座，值得落地。
  - 依据：ACE（AppWorld +10.6%、延迟 −86.9%）；治 brevity bias / context collapse。
  - 落点：`reset-ledger.ts`（新）、`types.ts`、`store.ts`、`sqlite-store.ts`、`orchestrator.ts`。
  - 单测：`tests/reset-ledger.test.ts`（9 用例：Generator/Curator/Reflector/端到端幂等/supersede）。
- [x] **P1-2 重置阈值标定 + 任务边界增量整理**（调研 §2.2，2026-08-31）
  - 已做：`needsAutoReset` 增加第二维——`resetThresholdFor(slot)` 按内容相似密度选阈值：
    密度 ≥60（review diff 密集）→ 50% 低阈值；否则常规 70%。密度代理指标 `content_density_pct`
    在派发时按「注入的 diff/地图字符 / spec 总长」计算并落盘到 slot + task_context 事件。
  - 「任务完成时轻量增量入账」已由 P1-1 的 Generator 覆盖（任务 done 即入账，不再撑到阈值整写）。
  - 量化（`scripts/measure-reset-threshold.mjs`）：review 注入 diff 占比 ~100（密度高→50% 阈值），
    implement spec 主导密度 ~0（维持 70%，不误触、不牺牲会话复用收益）。
  - 依据：Context Rot（远早于窗口满就退化）、Acon 阈值消融、SelfCompact（−30–70%）。
  - 落点：`types.ts`（常量 + slot 字段）、`session-tiers.ts`（resetThresholdFor）、`orchestrator.ts`（密度落盘）。
  - 单测：`tests/dispatcher-session-tiers.test.ts`（3 新用例）+ `tests/orchestrator.test.ts`（1 新用例）。
- [x] **P1-3 重置后 verbatim 近期窗口**（调研 §2.3，2026-08-31）
  - 已做：`session-tiers.ts` buildRecentWindow（RECENT_WINDOW_SIZE=3）——重置摘要后把该员工
    在途任务最近 N=3 条原始事件（worker_progress / steer_queued / task_question / agent_relay）
    逐字注入；按 task_id + slot_id 过滤 → 任务结束自动清空，不跨任务不叙事，不违 S5。
    `orchestrator.ts` 档位 C 重置块接线（无事件则注入空窗口）。
  - 量化（`scripts/measure-recent-window.mjs`）：近期窗口 ~38 tok（<注入摘要 1%），
    收益=堵住「重置后断片」（保留在途 steer/工具结果/问答，避免重复澄清/重跑测试）。
  - 依据：Claude Code auto-compact 混合策略、CaT 三层。
  - 落点：`session-tiers.ts`（buildRecentWindow）、`orchestrator.ts`（重置块接线）。
  - 单测：`tests/dispatcher-session-tiers.test.ts`（3 新用例）。A/B：重置后首轮澄清率/错误率。
- [x] **P1-4 N2 记忆注入——基础接线（已完成）**；深化 ① ✅ ② ✅（2026-09-01）③ ⬜
  - 已做：派发按槽位/团队注入相关记忆（importance≥3、有界 ≤6、指针式，`memory_injected` 标注）。
  - 深化① ✅ **检索升级 top-k BM25**：新增 `src/core/memory-rank.ts`（ASCII 词 + CJK bigram 分词；
    BM25 候选池内 IDF + tag 命中加成 + importance 加权；门槛=查询词重叠 / tag 命中 /
    importance≥4 任一，无信号不入选）。orchestrator 候选池放宽（去 importance 硬门、扩到
    `MEMORY_CANDIDATE_POOL=32`/owner），任务文本（title+spec+skill_tags）做查询。
    效果：低重要度强相关的记忆越过旧硬门入选且排前（旧代码只会注入高重要度但无关的）。
    单测 `tests/memory-rank.test.ts` +8 + orchestrator 集成 +1。
  - 深化② ✅ **reflection 冲突消解（Mem0 方法论）**：新增 `src/core/memory-conflict.ts`
    ——`bigramDice` 相似度 + `conflictCandidates`/`resolveConflicts` 纯函数。触发面：同 owner +
    **同 type**（跨 type 互补事实归 supports，判冲突会误伤）+ 共享标签 ≥2 + 内容相似度 ∈ [0.4, 1)；
    收口：contradicts 边（旧→新）+ 旧记录 importance 降为 1 退出活跃注入位（保留记录/边/历史，
    不删除数据）；已有 contradicts/supports 边视为已裁决（幂等）。**顺序约束**：冲突 pass 先于
    supports pass，否则同话题对先被 supports 标记抢占。单测 `tests/memory-conflict.test.ts` +8。
  - 深化③ 🔶→⬜ **记忆 bakeoff 扩样本（2026-09-01 跑满 10 pairs，结论落定）**：`scripts/memory-eval-code.mjs`
    写码型 10 pairs 全部完成（both done 10/10）：
    - **token：7/10 记忆组更省，平均 −2,902 tok（≈−15%）**；wall：5/10 无差异（+15.4s 噪声级）；
    - **符号检验 p=0.344 不显著**（与纯文本 10 对 p≈0.17 同族：方向稳定正向、样本不足）；
    - 结论按 bakeoff 纪律：**如实记录「方向正向、未达统计显著」，暂缓继续扩**
      （假设真实胜率 0.7，需 ~30 pairs 才可能显著，成本 3h+，投入产出比低）。
    - 报告：reports/memory-eval-code/summary.json + partial-{0-2,2-10}.json；bug 修复 dc3ccfb。
- [x] **P1-5 review hunk 两级加载 + 截断元数据**（调研 §1.3，2026-08-31）
  - 已做：`src/core/diff-hunks.ts`（parseDiffHunks / buildChangeMap / scoreHunk / selectHunksByBudget / buildTwoLevelDiff）。
    orchestrator review 注入改两级：变更地图恒全量（第一级）+ 相关 hunk 按预算装入（第二级，确定性关键词/路径匹配）；
    task_context 事件带 `review_truncated_hunks` 元数据（被切掉的 hunk 标题清单）。
  - 量化（`scripts/measure-review-hunks.mjs`）：119K diff（8 文件/87 hunk）下，旧 40K 定长截断
    **丢 5/8 文件、61/87 hunk、拦腰截断且无元数据**；两级加载地图恒全量（文件 0 丢失）+ 相关 hunk 优先。
  - 依据：Agentless（$0.34 vs $4.19）、SWE-Edit（−17.9%/+2.1%）、SWE-Pruner。
  - 落点：`diff-hunks.ts`（新）、`orchestrator.ts` review 注入；单测 `tests/diff-hunks.test.ts`（12 用例）。
  - 验证：review token/缺陷发现数/误报率 A/B（复用 bakeoff 基建）。
- [x] **P1-6 pinned 安全层逐字不变量测试**（调研 §2.5，2026-08-31）
  - 已做：导出 pinned 层模板（`COMMIT_DISCIPLINE` / `FALLBACK_IDENTITY`），新增 `tests/pinned-invariants.test.ts`
    （5 用例）：交付纪律/身份/路径白名单提示与模板逐字相等；叠加动态内容（tenets/记忆/steer/重置摘要）
    后 pinned 层 hash 不变；与 P0-2 前缀 hash 合并（同类型 static 前缀恒定）；review 任务同样保 pin。
  - 依据：TypeCompact（安全规则 pin 保真 2–4×）。
- [x] **审计 P1 · 正常流转 handoff 协议对齐**（2026-08-31）
  - 已做：常规「implement→review」流转补 `handoff_created` 事件（`H-REVIEW-*` 前缀，非持久化 handoff，
    仅事件留痕）——带 from/to/targets（含 commit）/verify 清单（diff_range_valid / test_log_exists /
    report_fields_complete），让「谁传了什么给谁」在协议层可见，前端可据此呈现传递链。
  - 不改传递机制（CR-03 审查者无仓库权限是硬约束，diff 仍宿主注入）；只把既成事实落成与协议一致的审计记录，
    避免「reassign 走协议、常规流转裸奔」的漂移。
  - 单测：`tests/orchestrator.test.ts`（1 新用例：常规流转 handoff_created 带 verify 清单）。
- [x] **审计 P1 · `/assets` 前端接线**（2026-08-31 已做，当晚复核确认）
  - `api.ts:157 fetchAssetText(path)` + `PodPanel.ts` 上下文查看器的「产物路径 → 查看产物」按钮，
    点开按 path 拉取文件内容（受 worktree 根白名单限制）。此前标为未做是标注滞后。
- [x] **审计 P1 · MCP 工具描述静态审查（安全）**（2026-08-31）
  - 已做：`mcp-gateway.ts` 新增 `scanToolDescriptions` 纯函数 + `McpGateway.scanTools()` 聚合入口。
    三类风险命中：prompt_injection（忽略/无视先前指令、新指令、jailbreak 等 high）、external_url（外联
    URL 潜在外传 medium）、privilege_verb（rm -rf / DROP TABLE / chmod / sudo 等 medium），每类带证据原文。
  - 接入第三方 server 前调用（接入方决定是否阻断注册），不阻断网关运行。
  - 单测：`tests/mcp-gateway.test.ts`（5 新用例：干净工具零误报 / 注入 high / URL / 越权动词 / scanTools 聚合）。

## 3. P2 —— 协议整洁度与未来跨机准备

- [x] **P2-1 ADOL 四招（2026-09-01，两招落地两招评估）**：
  - ✅ **schema $ref 去重（源码级）**：新增 `src/core/mcp-schema.ts` 共享片段单一源（slotShape /
    planTaskShape / slotId / approvalId / vendor / taskType / TOOL_BRIEFS）。pod_launch.plan 与
    pod_plan.tasks 的节点对象同源防漂移（type 枚举按语义收紧：launch 6 类 vs plan.add 5 类）。
    如实说明：MCP SDK zod→JSON Schema 内联序列化（$refStrategy='none'），**不产出 $ref 字节级去重**；
    价值在源码单一源 + 为未来 $refStrategy='root' 打底。
  - ✅ **tools/list --short**：`makeMcpServer(service, { toolListing: 'short' })` 入参 schema 置空，
    完整 JSON Schema 走 `pod_expand_tool` 按需展开；运行时入口 `POD_MCP_TOOL_LISTING=short`
    （mcp-bridge stdio + mcp-http 库层 opts/env，缺省 full 客户端无感）。
    量化（`scripts/measure-mcp-tools.mjs`）：full 3,476 B → short 1,198 B（**−65.5%**），
    展开 pod_launch 单工具 1,477 B。
  - ⬜ **requireOutput 字段投影**：收益依赖客户端按需取 outputSchema 的实际行为，暂无实测场景，评估后再做。
  - ⬜ **tag 检索式工具选择**：TOOL_BRIEFS 已带 tag（orchestration/read/approval/lifecycle/planning/memory）
    作单一源打底；真正的「按 tag 装载子集」需宿主侧动态工具呈现（与 P0-1 剩余项同源，宿主依赖）。
  - 单测：`tests/mcp-schema.test.ts` +6（共享片段序列化 / 枚举收紧 / short 置空+展开 / full 兼容）、
    `tests/mcp-http.test.ts` +1（HTTP 面 short 端到端）。
- [x] **P2-2 worker 结构化错误信封**（调研 §3.2）：新增 `src/core/error-envelope.ts`（`WorkerErrorEnvelope` 有界信封 + `faultFromEnvelopeCode` 短码前缀分流 + `parseErrorEnvelope`/`makeEnvelope`/`lastErrorEnvelope`）；`claude-headless` 侧 `claudeErrorEnvelope` 从 result/apiStatus、api_retry 尾记录、退出码构造信封并附到 `WorkerCompletion.error_envelope`；`task-machine.classifyFault` 信封优先于 stderr 正则（未知短码 fail-closed 回落）；编排器 `failed` 分支透传信封。stderr 12 行降级为纯调试材料（给机器短码、给人留全文）。
- [x] **P2-3 A2A 对齐 v1.0**（2026-09-01，调研 §3.3；对照官方 v1.0.0 规范逐项落地）：
  - ✅ **Push Notification（webhook 回调）**：新增 `src/a2a-push.ts`（注册 → 轮询 → 终态投递）；
    `sendMessage`/`sendMessageStream`/JSON-RPC 三入口都接受
    `configuration.pushNotificationConfig`（`{url, token?, authentication?}`，解析 fail-closed）；
    终态 POST StreamResponse 单键 `statusUpdate`（与流帧同构）；鉴权 authentication →
    `Authorization` 头，token → `X-A2A-Notification-Token`（JS SDK 默认头名）；
    at-least-once（2 次 / 10s 超时 / 3s 间隔），失败仅 stderr，**回调是旁路不影响编排**；
    mission 被替换即作废不误投。Agent Card `capabilities.pushNotifications=true`。
    SSRF 防线：只放行 http(s) scheme + 超时（v1.0 明确「服务端不得盲信 webhook URL」；allowlist 属部署侧）。
  - ✅ **input-required / rejected 显式映射**：`task_rejected`（能力拒单）与 `task_escalated`
    （转人工/attempts 烧尽/early exit）→ 非终态 `input-required` status（停顿需人工）
    + 审计 artifact。mission 未终态不能标 rejected-final（可重规划），input-required 即 v1.0 停顿语义。
  - ✅ **Agent Card 签名扩展位**：v1.0 `signatures`（JWS 数组）字段落位，loopback NoAuth
    如实给 `[]`；跨机/上网络时填真实签名（防发现环节投毒的第一道）。
  - **附带**：`PodService.eventsAfter` 投影补 `mission_id`（此前混流无归属，订阅方无法过滤）。
  - 测试：`tests/a2a-push.test.ts` +4（真实本地 webhook 端到端：投递体/鉴权头/重试放弃/
    mission 替换作废）、`tests/a2a.test.ts` +5（config 解析 fail-closed / 鉴权头 /
    拒单与转人工映射 / card 扩展位）。**测试写法教训：webhook 到达断言用条件等待**
    （waitFor 轮询 2s 上限），固定 sleep 在全套并行争用下必偶发——本项目 flaky 教训再现。
- [x] **P2-4 自然语言压缩小实验**（2026-09-01，**负向结论，按纪律停**）：新增 `scripts/compress-eval.mjs`
  （6 段真实项目文本快照 × 压缩+自评两步，claude 实测）。**平均压缩率仅 9%**（保持分 9.0/10 但远低于
  40% 门槛）——本项目叙事性技术文本（README/CR 叙事/commit message/评估结论/IM 描述）已接近信息
  密度上限，无可压缩空间。**结论：不做接入**（channel-im 出站摘要原样投递）。负向结论完整留存
  （reports/compress-eval/summary.json），与「知识型问题平局」同类。
- [x] **P1 feedback 环 v2：LLM worker 咨询**（2026-09-01，experiments 'feedback-consult' 灰度默认关）
  - planner.ts 新增纯函数：`bestConsultSlot`（capabilities∩缺口标签交集最大者，交集 0 也取 id 最小，
    边界交 LLM 判断）+ `buildConsultPrompt`（缺口标签 + 槽位能力 + spec 截断 1.5K + 禁工具指示）；
  - orchestrator：语义类拒绝分支灰度开启时 `consultPlannerFeedback`（deps.consult 注入式）真咨询
    相关 worker，结果裁 500 字符注入 spec；**咨询失败 / 无槽位 / 未注入 → 回落 v1 名册反馈**
    （feedback_mode: 'consult' | 'roster' 落事件）；灰度关 = 行为与 v1 逐字节一致；
  - pod-service 组装层实现 `consultClaude`：`claude --print --max-turns 4 --allowedTools none`，
    60s 超时。**实测踩坑**：`--max-turns 2` 下 claude 内部多步直接报 "Reached max turns (2)" 退出
    （压缩 prompt 单步成功掩盖了此问题）；禁工具 + 4 turns 后实测 32.3s 返回 342 字高质量建议
    （「大部分可做，缺生产凭证 → 建议拆分任务改述」，v1 名册反馈给不出这种执行侧洞察）。
  - 单测：planner +2（槽位匹配/prompt 构造）、orchestrator +3（咨询写回/失败回落/灰度关等价）。
- [x] **审计 P2 · channel-im 服务面接线**（2026-08-31 深夜）：新增 `src/im-http.ts`（webhook HTTP 交付层——`POST /webhook/{slack,lark}` 读**原始 body** 交 `verifyAndParseIm` 做 vendor 验签 + 时间窗防重放 + 飞书明文 verification token 鉴权；挑战握手原样回显；事件 id 重放去重；指令路由仍走 `channelTarget('channel')`，审批不绕过状态机；验签失败一律 401 fail-closed；出站无 bot token 时仅 stderr 打印 + ack；库层 `listenImHttp` 非 loopback 无 token 拒绝启动）+ `scripts/im-http-server.mjs`（`POD_SLACK_SIGNING_SECRET`/`POD_LARK_ENCRYPT_KEY`/`POD_LARK_VERIFICATION_TOKEN`/`POD_IM_TOKEN` 注入）+ `tests/im-http.test.ts`（11 用例）。`pod-service.channelTarget` 公开并参数化 `source`（'cron'/'channel'）审计溯源。mcp-gateway **客户端聚合仍需编排层集成**，大工程单独排期（诚实标注不虚标）。
- [x] **审计 P2 · Q 版形象小尺寸可读性**：`Avatar()` 内按 size 降级——`chibiHeadArt` 抽取头发/脸/五官头部可读层，`size < CHIBI_HEAD_ONLY_THRESHOLD(28)` 时丢弃身体/四肢/腿/道具/星（整身 14 层在 18~26px 下是散点噪音）；大尺寸与经典动物不受影响。角色识别色（头发色）保留。
- [x] **审计 P2 · `tierDefaults(vendor)` 死代码**（2026-08-31 晚处理）：改为由 `DEFAULT_SESSION_TIERS`
  常量派生（`return DEFAULT_SESSION_TIERS[vendor]`），删除与常量重复的那份 switch。
  选择派生而非删函数：测试仍引用它，且保留一个具名入口比散落查表可读。
- [x] **后续计划 P3 · ACP 消歧义 + 重估 Zed ACP**（2026-09-01）：新增 [docs/消歧义-ACP-2026-09-01.md](消歧义-ACP-2026-09-01.md)——
  钉死两个同名 ACP：IBM Agent Communication Protocol **已于 2025-08 并入 A2A**（A2A v1.0 于 2026-03-12 发布，webhook/长任务语义即本项目 a2a-push.ts 的实现）；Zed Agent Client Protocol 是「编辑器↔编码 agent」驾驶舱层（LSP for AI），ACP Registry（2026-01）已有 claude-code-acp / codex-acp / opencode 等现成 adapter。
  重估结论：**现成通道、宿主侧接线项（同 P0-1 剩余），无真实场景需求前不排期**——与自研 Web 控制台重叠，ROI 不如继续打磨 A2A 协议面。
  顺带修正 docs/adapters.md（明确 ACP=Zed 版 + 重估结论）与 docs/差距审计-vs-方案书.md 一条**错误审计结论**（把 IBM 合并错套到 Zed ACP 上，已撤回）。
- [ ] **后续计划 P3 · satellite 对齐 A2A Agent Card**（v0.4 方向，先记录不急改）：接 Agentforce/Now Assist/Azure AI Foundry/Bedrock AgentCore 的通道。
- [ ] **后续计划 P3 · AG-UI 参考（可选）**：SSE 帧与 AG-UI 同构，未来支持第三方前端可省适配。

## 4. 本轮会话明确的可选后续

- [x] **员工侧 `pod_mem_*` 工具进 worker**（2026-09-03，commit e8c556b）：standalone 挂 /mcp 路由 + listen 后写 worker-mcp.json（存在性即灰度开关）+ claude-headless mcpConfigPath 注入 --mcp-config 与 pod_mem_* 白名单 + mcp-server 补挂三件套注册（TOOL_BRIEFS 早已宣告但 fullShapes/registerTool 缺失——v9 实证模型按清单调用必 not found 的真根因）。v9 真实 mission 实证：claude worker 经 MCP 调 pod_status/pod_expand_tool/pod_mem_write，memory_records 落库 owner 归属正确。单测 +3，全量 876 passed。遗留观察：claude -p 在 MCP 工具探索后偶发无 MISSION_REPORT 退出（silent_failure），模型行为侧另记。
- [x] **记忆注入扩大样本验证（2026-09-03 阶段性收口，标记暂缓不虚标）**——bakeoff 已跑满 10 pairs（写码型），方向稳定正向但符号检验 p=0.344 未达显著（详见 P1-4 深化③）；跨 mission 注入链路已在 v11 真实 mission 实证命中（6a0cea7，全库候选路兜底）。剩余「扩到统计显著」需 ~30 pairs / 3h+，投入产出比低，按 bakeoff 纪律**暂缓**。下一步方向：BM25→向量召回（解决字面重叠天花板），非单纯扩样本。

## 5. P4/P5 —— 健壮性 / 体验 / 文档（低优先，随手可做）

- [x] **flaky 测试排查**（2026-08-31 晚第一轮 + 2026-09-01 上午第二轮，结论有修正）
  - 现象：全套 714 用例偶发假失败，每次落在不同文件。08-31 落在
    apply-patch / launch-atomicity / verifier；09-01 落在 fs-browse / orchestrator（重试用例）。
  - 量化（`scripts/bench-git-fixture.mjs` 已扩展为串行 + 并行双模式）：
    串行 fixture **中位 315ms / p90 411ms**；**8 路并行（含 worktree add）单 fixture 峰值仅
    599ms**——对 5s 阈值仍有 8× 余量。**git fixture 不是慢源**（连并行争用都排除了）。
  - 真正的慢源是 `fs-browse` 305-dir 测试：**真实 mkdir×305** 在全套并行争用下实测 5.2s/8.1s，
    撞 5s testTimeout。处置：把截断逻辑抽成纯函数 `capEntries`，测试改用合成 305 项名
    （**61ms**，不再碰真实文件系统），端到端 wiring 由「未超限」小用例兜住。
  - 残留：orchestrator 两个重试用例（「attempts=1 重派成功」「连续失败 3 次」）仍在全套
    并行下偶发失败，单独跑 72/72 全过。是 microtask/timer 在 16 worker 争用下的时序抖动，
    非 git fixture 慢、非逻辑缺陷（隔离下确定性通过）。**未调阈值**——与 08-31 结论一致：
    给 599ms 的操作配 30s 超时仍是 cargo cult。留待按需深挖。
- [x] **`fs-browse` 300 条上限加截断提示**（2026-08-31 晚）
  - `BrowseResult` 新增 `total` / `truncated`；`childDirs` 返回过滤后总数而非静默 `slice`；
    截断逻辑抽纯函数 `capEntries`（可离线测边界）。
  - 前端超限时显示黄色提示「仅列出前 N 项（共 M 项）。若找不到目标，请手动输入完整路径」。
  - 单测 `tests/fs-browse.test.ts` +2（截断必须上报 / 未超限不误报），纯函数化后 <100ms。
- [x] **会话删除 / 重命名**（2026-09-01，此前无 deleteMission，会话只增不减）
  - `PodStore.deleteMission(id)`：JsonStore/SqliteStore 双后端按 mission_id 级联清空
    missions/slots/tasks/handoffs/ledger/reset_entries/approvals/events（SQLite 处理了
    tasks 复合键、ledger 无业务主键按 seq、events 复合主键）。
  - `PodService.deleteMission` 守卫：非终态会话拒绝（MISSION_ACTIVE，先 abort 再删）；
    best-effort `git worktree remove --force` + 清理 `~/.dsh/pod/missions/<id>` 数据目录。
  - `PodService.renameMission` 复用 `updateMission`（会话名 launch 即定不可改的历史遗留）。
  - 路由：`DELETE /api/dsh-pod/missions?id=` + `POST /api/dsh-pod/missions/rename`；
    前端 `api.ts` 加 `deleteMission`/`postRenameMission`，`SessionsView` 详情面板加
    重命名/删除按钮（活跃会话删除按钮 disabled，删除前二次确认）。
  - 单测：store +1（级联删除/相邻 mission 保留/NotFound）、sqlite +1、routes +6。
- [x] **`missionArchive` 事件按字节设上限**（2026-09-01，此前 `slice(-500)` 条数有界但字节无界）
  - 抽纯函数 `capEventsByBytes(events, maxBytes)`：从最近的事件往前累加序列化字节，
    超预算即停、始终保留最近一条（单条超预算也不返回空）、返回时间正序；预算
    `MISSION_ARCHIVE_EVENTS_MAX_BYTES = 256KB`。
  - `missionArchive` 的 `events` 从 `slice(-500)` 改为 `capEventsByBytes(...)`；
    极端 500×8KB ≈ 4MB/次 被压回 ≤256KB。
  - 单测：pod-service +4（纯函数边界）、sessions +1（missionArchive 集成字节断言）。
- [x] **store tmp 残留 EPERM**（2026-09-01，实测复现后确认是真实缺陷，非「可忽略的噪音」）
  - **实测**：Windows 上 rename 到被占用/目录路径稳定抛 `EPERM`，旧实现在 rename 阶段抛错后
    `xxx.tmp-<pid>` 永久留在数据目录（一次失败留一个，只增不减）。
  - 抽 `src/core/atomic-write.ts` 共享内核（`store.json` 与 `memory.json` 此前各写一份、
    有同一缺陷）：`atomicWrite` 失败时**清掉 tmp 再原样重抛**——落盘失败必须上抛
    （静默吞掉 = 上层误以为写成功，比残留危险得多），但绝不留残骸。
  - `sweepStaleTmp(dir, prefix)`：`open()` 时清理崩溃进程遗留的 tmp，**只删 pid 已死的**
    （活进程的 tmp 可能正处在「写完未转正」窗口，删了会破坏它的原子写；最坏结果只是
    「这次没清掉」即维持现状，失败方向安全）。
  - 单测 `tests/atomic-write.test.ts` +11（两条 EPERM 复现路径、pidAlive 边界、
    JsonStore/MemoryStore 接入点）。
- [x] **方案书补本轮 CR 记录**（2026-09-01，变更以设计变更记录追加文末）
  - 方案书此前停在 CR-37（08-28），08-29 至 09-01 共 **15 个提交无记录**。补齐
    **CR-38~CR-45**（1380 → 1525 行），全部以提交说明与实测数据为据，不作推演：
    CR-38 standalone 独立控制台 / CR-39 控制台重设计+会话中心+引擎可靠性安全 /
    CR-40 真实模式实证批次 / CR-41 生命周期状态机+A2A 协议面 / CR-42 上下文生命
    周期与协议开销 P0 / CR-43 前端可达性补齐 / CR-44 工具分层+静态前缀+上下文生命
    周期+协议安全 / CR-45 成本可见性+会话管理+原子写修复。
  - **顺带修掉两个真实缺陷**：
    1. README 自 08-28 起引用「独立运行（standalone 控制台，CR-38）」，但方案书
       根本没有 CR-38——**引用了不存在的变更编号**。现已在方案书补齐该条。
    2. README:12 与 AGENTS.md:4 的方案书链接 `../../DSH-Pod-项目方案书-v2.0.md`
       解析到 `D:\`（不存在），实际文件在 `D:\玩具\`——改为 `../`。
- [x] **README 补新接口**（2026-09-01）
  - 新增「HTTP API 面」小节：按 会话/编排、审批、事件流、会话中心、规则、任务级、
    记忆、Cron、文件系统、联邦/A2A 分组的完整路由表；覆盖此前缺的 `/rules DELETE`、
    events `after_id` 分页（`cursor`/`has_more`）、`pause`/`resume`、会话删除/重命名、
    `task/pause`/`task/resume`/`reassign`、`memory`/`memory/correct`、`cron`、
    `plan`、`approvals/detail`、`fs/browse`、`assets`。
  - 修正「全部 12 条路由」过时表述；tests badge 480 → 727。
- [x] **byStage 接入前端**（2026-08-31 晚复核：早在此前批次已完成，`PodPanel.ts:342` 传
  `ledgerByStage` → `chat-view.ts:479-488` 渲染「阶段归因」，按 token 降序带美元成本。
  此前标为未做是排查时 grep 被 `head` 截断导致的误判）
- [x] **失败路径单独计数**（2026-09-01，成本可见性第二维）
  - ledger 每笔 usage 带 `attempts`（0=首派，≥1=重试）；`summary` 新增 `byAttempt` 桶
    （与 byStage 正交），重试成本 = 除 '0'/'unknown' 外桶之和。orchestrator 记录时传
    `task.attempts`（recordUsage 在 fail 前调用，语义=「刚跑完这次」的尝试号）。
  - 前端侧栏「Token 明细」下新增「重试成本」行（N 次失败尝试 · tokens · $），有重试才显示。
  - 单测：ledger +4（分桶/求和/老条目归 unknown/守恒）+ orchestrator 重试断言 attempts=[0,1]。
- [x] **信息增量式止损 early exit**（2026-09-01，补「是否还在产生新信息」维度；experiments 灰度）
  - 新增 `src/core/failure-evidence.ts`：`failureSignature(fault, message)` 确定性指纹
    （空白折叠 + 小写 + 尾段 400 字符；**不去数字**——退出码/行号本身是证据，宁可少触发不误杀）。
  - `task-machine.applyFailure`：比较本次与上次失败签名，连续同证据达阈值（默认 2 →
    第 2 轮与第 1 轮同签名即止损）且 experiments `early-exit` 开启 → 不烧最后一轮全价重试
    直接转人工（attempts=3 兜底在前，阈值的意义就是省下第 3 轮）。
    事件带 `early_exit` / `no_new_evidence`；Task 新增 `last_failure_signature` /
    `no_new_evidence` 落盘字段（SQLite JSON 行免迁移）。
  - 边界：软失败（429/need_clarify）与凭据失败不参与比较也不清空硬失败证据链
    （429 是瞬态，跨过它仍算连续同证据）；灰度关 = 行为与旧版逐字节一致。
  - 编排器接线：`earlyExit: { isEnabled: () => experiments.isEnabled('early-exit') }`，默认关。
  - 单测 `tests/early-exit.test.ts` +7（指纹归一化 / 灰度关旧行为 / 止损触发 /
    证据变化不触发 / 429 跨越链保持 / 阈值可调）。

---

**建议执行顺序**：P0-1/P0-2（固定税，先做）→ P1-1（delta 账本是 P1-2/3/4 公共底座）→ P1-5（review token 大头）→ 其余 P1/P2 按排期。每项沿用 bakeoff 先量化。

---

## 2026-09-01 晚间 · standalone 驾驶舱双 harness 实测（claude × codex/ark）

- [x] **独立驾驶舱验证**：`node dist/standalone-server.js` 浏览器直开 PodPanel（UI/REST/SSE/A2A 全通），零 DSH 依赖——「驾驶舱操控所有 harness」即产品形态。
- [x] **codex 接入（3 个真 bug 全修）**：① detect 只读 stdout（非 TTY 下 login 状态在 stderr）→ 合并两路（ef0dafb）；② rejectTerminal 守卫漏 ready（协商换人后无下家 crash）→ 允许 negotiating|ready（27186a5）；③ standalone 装配漏注册 ark → 补 makeArkBackend（c321919）。
- [x] **codex 0.148.0-alpha.9 上游缺陷（记录）**：skills context budget 超限（23+ 内置 skills，config override 三格式无效）→ 执行必失败；code-mode-host.exe 路径不匹配已复制修复。等 CLI 新版，适配器本身验证可用（探测/协商/接受/派发全通）。
- [ ] **v6 mission 收尾（进行中）**：claude 实现 + ark 审查，T-1 遇模型偶发 no-report 重试中；预期走到审批卡（用户在驾驶舱点批准 → 合并 → done）。

- [x] **need_clarify 软失败竞态收口（2026-09-03，commit ae1bfcc）**：真根因不是时序竞态而是**升级判定缺口**——软失败不烧 attempts（恒 0），applyFailure 只按 attempts>=3 判升级，worker 反复 need_clarify 时 shouldRetry 恒 true → driveLoop 无限自动重派（回退验证实证：FakeBackend 微任务时序下重派循环饿死事件循环，vitest 超时都无法中断，比 09-02 观察的「卡 running」更极端）。修复 = needClarifyExhausted（need_clarify && soft_attempts>=3 → escalated 转人工，烧钱封顶）；保留自动重派（DemoBackend 链路/sequential-missions/无人值守 cron 依赖「提问→重派→二次干活」自动化，steer 答复在重派时注入 spec，问答闭环不变）。单测 +3，全量 879 passed。

---

## 2026-09-02 下午 · 桌宠房间（学习 dsh-web/dsh-pet）+ 后续待办

- [x] **桌宠房间视图（commit 1777b55，已推送）**——按用户预设实现「大房间多桌宠」：每个 harness
  （dsh/claude/codex/ark/opencode）一只鲸鱼娘桌宠，实时动画展示工作状态。
  - 学习 dsh-pet 精髓并移植：CSS background-position 帧动画引擎（rAF 推进，无 Canvas/三方库）、
    9 轨动画契约（idle/左右跑/挥手/跳/趴下/等待/跑动/翻阅）、phase→animation 序列轮换。
  - 新增 `src/web/pet-sprite.ts`（引擎）、`pet-atlas-data.ts`（鲸鱼娘 atlas base64 内联 2MB/32段，
    来源 dsh-web assets/whale，BSD-3-Clause）、`pet-room.ts`（房间视图 + slotPhase 状态映射）、
    `tests/pet-room.test.ts`（23 用例：帧定位/phase 映射/序列推进/slotPhase/SSR 防黑屏）。
  - 修改 PodPanel（rail「桌宠房间」paw 图标）/console-css（深海房间场景）/icons；顺手修复
    console-css 重复 `.dsh-note.demo`。全量 868 passed / 1 skipped，typecheck 0。
  - UI 形态：气泡（任务+进度+错误）、名牌（vendor/role/token 成本）、vendor hue-rotate 品牌区分。
- [ ] **桌宠房间增强（可选方向，按需排期）**：
  - 多套桌宠皮肤按 harness 换皮——dsh-pet 仓库还有 miku/ouo-neko/starry-doll 等资产
    （assets/ 下多目录，各带 pet.json + spritesheet），换皮 = 换 atlas data + 帧契约，引擎复用。
  - 桌宠点击交互——戳桌宠看任务详情/操作（当前气泡只读展示，可点开 context 面板 / steer）。
  - room 布局增强——多房间分区（按 mission/role 分组）、缩放/拖拽、宠物间相对位置动画。
- [x] **need_clarify 软失败竞态（2026-09-03 收口，commit ae1bfcc，见 §竞态收口）**：根因是升级判定缺口（软失败无上限），非时序竞态。
- [x] **双 harness 真实链路闭环（2026-09-03，v7+v8 两个 mission 实证）**：
  - v7（claude 实现 + ark 审查，clampInt）：协商→实现 done（2e7bf26，测试 16/16）→ ark 审查
    抓到真 blocker（out/*.diff 产物混入 commit）→ 触发 ark crash bug（见下条）→ escalated →
    人工裁决 done → 审批卡批准 → 合并 afc1edc。
  - **ark 审查 crash bug 已修（a0a36b9）**：审查者输出枚举外 status（needs_changes，审查发现
    真 blocker）被 ark-headless 兜底 crash、审查意见整个丢弃。修复 = extractReport 归一化
    status（近义词表，未知值保守兜底 blocked）+ ark runOnce 对齐 claude-headless（report 拿到
    即 exit=done，分派交任务机）。单测 +2，全量 870 passed。
  - v8（claude 实现 + **codex 审查**，capitalize 任务）：**codex worker 首次真实 mission 跑通**——
    09-01 的 skills budget 缺陷未复现（换 do.top 通道后正常）。审查质量真实：codex 自行发现
    沙箱 EPERM 跑不了 node:test，改用「剥离类型标注 + 内联断言」复验 6/6 通过。
    实现 done（ad4d4db 合并）→ 审查 done → 审批卡批准 → mission done 全闭环。

**当前全局状态（2026-09-03 更新）**：代码健康（typecheck 0、全量 891 绿、工作区干净与 origin 同步）。
代码待办 = 桌宠增强（分区+详情卡 ✅ e07629d；多皮肤待 atlas 资产）+ 员工侧 mem 工具 ✅ e8c556b +
记忆跨 mission 闭环 ✅ 6a0cea7（v11 实证）+ 单槽独立审查可行性裁决 ✅ cb10797（v9/v11 根因闭环）+
A2A Agent Card / AG-UI（参考，可选）+ 记忆扩样本（暂缓，方向改为向量召回，见 §4）。

**环境实测（2026-09-03 上午，用户配好 codex 后终态）**：
- ✅ claude 通道（127.0.0.1:15721 代理）——实测 `/v1/messages` 正常返回（路由 glm-5.3）。
- ✅ codex 通道——用户已换代理商：dayaai（欠费弃用）→ `api.do.top/v1`，模型 gpt-5.6-sol →
  `GLM-5.3-Flash`，新 key（`sh_` 前缀，`~/.codex/auth.json` + `config.toml` wire_api=responses）。
  实测 `/v1/models`、`/v1/chat/completions`、`/v1/responses`（codex 实际 wire 协议）三端点全通。
  ✅ 遗留观察项已关闭：codex CLI skills budget 缺陷在 v8 真实 mission 中未复现（换 do.top 通道
  + 新配置后正常完成审查任务）。
- ✅ opencode 有本地配置（~/.config/opencode/）；dsh-subagent / DSH host v0.1.1-rc.2 可用；
  ark 无独立配置文件（走 DSH host 内置）。
- **结论：真实 LLM 双 harness 跑测与 v6 mission 收尾的环境阻塞全部解除。**
