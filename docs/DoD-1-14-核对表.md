# DSH-Pod MVP DoD 1–14 核对表（W6 发布候选）

> 状态日期：2026-08-25（Bake-off 运行期间快照；DoD-10 最终以 all-summary.json 为准）
> 依据：DSH-Pod-项目方案书-v2.0.md 4.2 节验收标准
> 每项标注：✅ 通过 / 🔄 进行中 / ⬜ 未做，附证据位置

---

| # | 验收标准 | 状态 | 证据 |
|---|---|---|---|
| 1 | DSH 输入 /pod，模态打开，CC/Codex 登录态显示正确（未登录灰掉 + 指引；CLI 版本已 pin） | ✅ | 侧边栏「Pod」入口 + Canvas 面板（src/web/client.ts 挂载）；preflight [3]/[4] 登录态检测 + 版本 pin（preflight.ts，CR-01-0 未装如实灰掉）；live 冒烟 tests/preflight.live.test.ts |
| 2 | 一句话目标后 10 秒内看到任务计划（plan.md 落盘 + Canvas 任务列表） | 🔄 | 默认链自动生成（CR-06-5）即时可见于看板；plan.md 落盘为 planner charter 输出契约（src/charters/planner.md），**待 Bake-off 现场计时确认** |
| 3 | 每次交接：事件流有记录、payload 可点开看四件套 | ✅ | 交接协议四件套（src/core/handoff.ts）+ 事件流（/api/dsh-pod/events，Canvas 右栏 2s 轮询）；事件 payload 完整落盘（store.appendEvent） |
| 4 | Codex 实现过程实时可见（文件变更 + 测试输出流式滚动） | ✅ | 事件流含 worker_progress（text/tool_call 流式入队）；Canvas 轮询展示；真实链验证（orchestrator handleCompletion + progress 事件） |
| 5 | 合并前必经独立 review：审查者 ≠ 实现者，只收到 diff + 规格 + 测试输出，无实现者叙事 | ✅ | tasksCompleted 质量门 fail-closed（mission.ts DoD-5 强制：review 必须 done、审查者≠实现者、有审查对象）；最小上下文 diff 注入（orchestrator.ts:372-385，CR-03）；单测 orchestrator「审查者槽位被占用时 review 不得派给实现者本人」 |
| 6 | approve 才落主树；落盘 diff 与报告一致；每任务独立 commit，diff = commit 区间 | ✅ | ApplyPatch 单入口（apply-patch.ts，不变量 3：卡必须 approved 才 merge）+ 串行锁；Verifier commit/区间一致性校验（verifier.ts）；实测：mission M-1787562478771-63687 approve → merge 9503e87，commit 4e11fcd 独立 |
| 7 | 成本数字：tokens 来自真实 usage；美元为显式标注的等效估算（含价目表版本号） | ✅ | Ledger 双列（ledger.ts）：usage_source=measured、price_table_version、equiv_usd 标注；Canvas 账本双列展示；实测 $0.043/109,518 tokens（含 price_known=false 如实标注） |
| 8 | kill 任意员工 → 状态机正确进入 blocked 不卡死；空闲超时/429 退避/凭据过期/流断裂符合故障表 | ✅ | 故障分类全集（task-machine.ts）；429 不计 attempts + 退避恢复；idle watchdog kill + blocked(idle_timeout)；单测全覆盖（orchestrator/watchdog tests） |
| 9 | 自然语言组队与预设组队产生的配置等价可改 | 🔄 | Team Builder 表单（goal/cwd/员工）→ 默认链；预设为将来版本；MVP 最小形态等价性**待现场验证**（等价 = 表单产物与 preset 产物同构） |
| 10 | Bake-off 报告发布：5 任务 × 2 条件共 10 次运行，含失败样本与负向结果，原始数据可复现 | 🔄 | 批次运行中（scripts/bakeoff-all.mjs → reports/bakeoff/all-summary.json + 各 runId.json/progress.log）；已复用 small-1-baseline（done 194.3s） |
| 11 | DSH 插件 + 浏览器重启后：mission 从磁盘恢复，awaiting_approval 审批卡重新呈现 | ✅ | docs/DoD-11-12-14-验收记录.md：自动化用例（store.test.ts）+ 两次真实宿主重启现场验证 |
| 12 | 会话生命周期生效：持久会话上下文超 70% 自动重置并按规格注入磁盘摘要；瞬时档每任务新进程 | ✅ | 同上验收记录：session-tiers.ts 三档 + 70% 阈值 + buildResetSummary；claude per-mission / codex transient；单测全绿 |
| 13 | 手动模式可用：watchdog 触发后，UI 可直接派发/收集/审批，不依赖 commander | ✅ | commanderFailed → orchestration_mode=manual（mission.ts）；Canvas 手动派发按钮 + steer + approve/deny + abort 全走代码入口（routes.ts，不依赖 commander LLM 会话）；commander 创建失败事件降级（pod-service.test.ts） |
| 14 | Windows preflight 全绿（CRLF/.gitattributes、git worktree 可用性、测试命令探测） | ✅ | 同上验收记录：preflight 十项 + repairPath/.cmd 回退/stdin 注入/UTF-16 容忍；单测 + live 冒烟 |

---

## 汇总

| 状态 | 数量 | 项 |
|---|---|---|
| ✅ 通过 | 11 | 1,3,4,5,6,7,8,11,12,13,14 |
| 🔄 进行中/待现场 | 3 | 2（10 秒计时）、9（预设等价）、10（Bake-off 运行中） |

## 结论

MVP 功能面已全部实现，DoD 硬性项 11 条已实证通过。剩余 3 项均挂在 **Bake-off 运行**上（DoD-10 是 2/9 的现场数据源）：批次完成后补 DoD-2 计时、DoD-9 等价性说明，即可发布 **MVP 发布候选 v0.1.0**。
