# DSH-Pod DoD 15–19 核对表（v2.1 增补，W6 发布候选）

> 状态日期：2026-08-26（v0.2 推进期快照）
> 依据：DSH-Pod-项目方案书-v2.0.md 4.2 节验收标准（v2.1 扩至 19 条）
> 每项标注：✅ 通过 / 🔄 进行中 / ⬜ 未做，附证据位置

---

| # | 验收标准 | 状态 | 证据 |
|---|---|---|---|
| 15 | **后端版本锁定生效**：preflight pin/check 三态（锁定一致放行 / 不一致拒绝 / override 绕过）单测通过，Bake-off 每条 run 可查到 worker_version | ✅ | `src/core/backends-lock.ts`（BackendsLock pin/check/override）+ `src/workers/preflight.ts`（--pin 记录实况 / --check 对照锁定）；MISSION_REPORT 元数据带 worker_version（report-schema.ts）；单测 tests/backends-lock.test.ts + tests/preflight.test.ts；CR-08 Berd-A（P0→MVP Must） |
| 16 | **报告契约单一事实源**：report-schema.ts 导出校验与提示词片段，schema 漂移测试通过，grep 无手写字段清单 | ✅ | `src/core/report-schema.ts`：zod 定义 MissionReport，导出 `validateMissionReport()` / `renderReportPromptFragment()`（类型、校验器、提示词片段同源）；单测 tests/report-schema.test.ts（schema 漂移/字段一致性）；CR-08 Berd-B |
| 17 | **Canvas 资产读取白名单**：资产端点只在 mission worktree 白名单内可读，穿越路径（`..`/绝对路径/盘符/符号链接）全拒绝，越界 403 | ✅ | `src/core/asset-whitelist.ts`（makePathWhitelist / resolveAsset：拒绝 `..`、绝对路径、盘符、符号链接、realpath 逃逸）+ `GET /api/dsh-pod/assets`（routes.ts，只读 mission worktree 根集合）；穿越单测 10 条（tests/asset-whitelist.test.ts，含注入式 fs 模拟 symlink/realpath）；CR-08 Berd-C / AS-4（DoD-17） |
| 18 | **审批规则层与 HITL 增强生效**：规则命中优先 / 只读命令自动放行 /「记住规则」后同类调用免弹卡 / 审批卡可编辑参数后放行，deny 原因回灌 worker | ✅ | `src/core/permission-rules.ts`（decidePermission：mission 级优先 global / bash 只读放行 / DEFAULT_RULES）+ `src/core/approvals.ts`（decide：AS-3 可编辑参数 edited_params 留痕、W4「记住规则」rememberRule、AS-2 rule-auto-* 生成）+ deny 原因回灌（orchestrator 以 steer 排队给 owner slot）；单测 tests/permission-rules 相关 + tests/approvals.test.ts；CR-08 AgentScope-A/B/C（DoD-18） |
| 19 | **事件流可重建员工完整回复**：按 slot+reply_id 聚合重建 = 原始回复（Canvas 重连/复盘不丢上下文） | ✅ | `src/core/events.ts`（emitWorkerProgress：进度事件带 reply_id+seq 落盘，同任务共享 reply_id、新任务重置游标；seq 保序可乱序重排）；单测 tests/events.test.ts（同任务共享 reply_id / 新任务新 reply_id）；CR-08 AgentScope-D（DoD-19） |

---

## 汇总

| 状态 | 数量 | 项 |
|---|---|---|
| ✅ 通过 | 5 | 15,16,17,18,19 |

## 结论

**DoD 15–19 全部达成**（v2.1 增补部分），与 DoD 1–14（docs/DoD-1-14-核对表.md）合计 19 条全绿。
可以发布 **MVP 发布候选 v0.1.0**。
