# DSH-Pod DoD 11/12/14 验收记录（W6）

> 验收人：开发会话（本机 Windows）
> 日期：2026-08-25
> 依据：DSH-Pod-项目方案书-v2.0.md 4.2 节验收标准（DoD-11/12/14）
> 仓库：D:\玩具\dsh-pod（commit eee8290 为验收基线，含 W6 全部修复）

---

## DoD-11：跨重启恢复（mission 从磁盘重建，awaiting_approval 审批卡重新呈现）

### 判定：✅ 通过（自动化测试 + 真实现场双重验证）

**1) 自动化测试（新增，tests/store.test.ts）**
- `DoD-11 跨重启恢复：关闭后重新 open，mission/任务/审批卡从磁盘原样重建`
- 构造 `awaiting_approval` mission + done 任务（commit_sha）+ pending 审批卡 → flush → 全新 JsonStore 实例 re-open
- 断言：mission 状态、任务 commit、审批卡 status/patch.worktree_path 全部从磁盘原样恢复；审批卡可继续裁决
- 结果：12/12 通过（含本用例）

**2) 真实现场验证（本次开发周期内两次宿主重启）**
- 场景：mission `M-1787562478771-63687`（running）→ T-1 escalated 卡死
- 宿主重启 #1（PID 2072 → 15524）：重启后 `/api/dsh-pod/status` 返回 mission 状态、T-1 escalated、T-2 ready —— 任务与事件流从磁盘恢复
- 宿主重启 #2（PID 15524 → 21256）：重启后 `awaiting_approval` 审批卡 `A-1787624936607-792658`（pending）重新呈现于 status API —— 与重启前完全一致
- 随后 humanResolve → approve → merge 全链走通，审批卡 approved，mission done
- 关键机制：JsonStore 磁盘唯一事实源 + ApprovalEngine.rebuildAfterRestart（pending 卡全来自磁盘，无内存态）+ MissionMachine 显式绑定 missionId（不猜"当前"）

---

## DoD-12：会话生命周期三档制（持久会话 70% 自动重置 + 磁盘摘要注入；瞬时档每任务新进程）

### 判定：✅ 通过（单元测试 + 代码路径核查）

**实现位置**
- `src/core/session-tiers.ts`：`tierDefaults`（claude=per-mission，codex/dsh=transient）、`estimateCtxUsage`（tokens/窗口，封顶 100%）、`needsAutoReset`（auto-reset 档且 ≥70%）、`buildResetSummary`（结构化摘要：已完成任务 title/commit/状态 + 进行中任务，**不含原始对话与叙事**）
- `src/core/handoff.ts`：`planDelivery` 按三档 × 交接模式输出确定性动作（含 `reset-session + inject: structured-summary`）
- `src/workers/claude-headless.ts`：per-mission 档用 `--session-id`/`--resume` 跨进程复用；瞬时档每任务新进程
- `src/workers/codex-headless.ts`：瞬时档等价新线程；exec resume 不可用 → 降级档 A（R1）

**自动化测试（tests/dispatcher-session-tiers.test.ts）**
- 默认档映射：claude=per-mission ✓ codex/dsh=transient ✓
- 占用估算：70% 边界（100k+40k / 200k = 70）✓ 封顶 100% ✓
- auto-reset 档 + 70% 阈值判定（69.9 不触发，95 触发）✓ per-mission 档 95% 不触发 ✓
- 阈值常量 == 方案书数值 70 ✓
- buildResetSummary：只含本员工 done 任务 + commit，不含他人任务，不含原始对话 ✓
- 结果：全绿

**运行期接线**
- orchestrator 每任务完成后 `estimateCtxUsage` 更新 slot.ctx_usage_pct（src/core/orchestrator.ts:511）
- 交接时按 planDelivery 矩阵执行（含 reset-session + 摘要注入路径）

---

## DoD-14：Windows preflight 全绿（CRLF/.gitattributes、git worktree 可用性、测试命令探测）

### 判定：✅ 通过（单元测试 + 真实 CLI 冒烟）

**实现位置**：`src/workers/preflight.ts` —— `runPreflight` 十项检测：

| # | 检测 | 状态 |
|---|---|---|
| 1 | node ≥ 22.5 | ok（22.x 实测） |
| 2 | git ≥ 2.30 + worktree list 可用 | ok |
| 3 | claude CLI 已安装 + 已登录 + 版本 pin | ok（auth status JSON loggedIn 实测） |
| 4 | codex CLI（未安装如实报告，不假装可用） | 如实 fail/warn（CR-01-0） |
| 5 | DSH subagent 能力（宿主原生） | ok |
| 6 | 目标 cwd 是 git 仓库 | ok |
| 7 | 磁盘 > 1GB（worktree 余量） | ok |
| 8 | 换行符策略 .gitattributes eol=lf | ok（缺失则 warn + worktree init 写入） |
| 9 | 测试命令探测（package.json → Makefile → CI） | ok |
| 10 | 能力覆盖体检（计划标签 ⊆ 员工能力） | ok（launch 时计算） |

**自动化测试**
- tests/preflight.test.ts：parseSemver/satisfiesMin（容忍 .windows.1）、parseClaudeAuth（JSON loggedIn + fail-closed）、detectTestCommand（三源降级）、checkCapabilityCoverage、ProcessRegistry —— 全绿
- tests/preflight.live.test.ts：真实 CLI 冒烟（claude 已装已登；codex 如实报告）—— 通过

**Windows 专项（CR-02/CR-03 实证累积）**
- repairPath()：宿主 PATH 被外部程序改写时幂等补回 node/git/claude 目录（CR-02-4）
- .cmd 包装器回退：execFile ENOENT → shell 执行一次（claude.cmd 分发形态）
- stdin 注入：绕过 cmd /c 中文 argv 8191 字符限制（CR-03-4）
- UTF-16 输出容忍：PowerShell `*>` 重定向产物（CR-02 实证）
- .gitattributes eol=lf + worktree init 强制（CRLF 治理）

---

## 结论

DoD-11 / DoD-12 / DoD-14 三项验收标准全部满足：
- **DoD-11**：自动化 + 两次真实宿主重启现场验证（审批卡跨重启重新呈现并完成裁决闭环）
- **DoD-12**：三档会话生命周期实现完整，70% 自动重置 + 结构化摘要注入有单测背书
- **DoD-14**：preflight 十项 + Windows 专项对策全部落地，单元与真实 CLI 双层验证

剩余 W6 项：Bake-off 全量 10 次（运行中，reports/bakeoff/all-progress.log）+ 汇总报告（DoD-10）。
