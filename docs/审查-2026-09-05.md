# 全面代码审查与修复记录（2026-09-05）

> 基线：main @ 288cf8e（与 origin/main 同步，工作树干净），`npm run verify` 全绿后开工。
> 方法：3 路并行深审（核心编排 / 存储+worker / 协议+HTTP 路由），26 个候选逐一在源码核实，
> 确认 25 处真实缺陷并全部修复；每处修复附回归用例（`tests/audit-fixes-0905.test.ts`，24 例）。
> 收尾：全量 verify 全绿（typecheck 0 / 943 tests passed）+ standalone demo 模式 HTTP 端到端全链路实证。

## A. 核心编排层（8 处）

| # | 严重度 | 缺陷 | 修复 |
|---|---|---|---|
| A1 | P1 | `dispatcher.ts` 的 `ACTIVE_TASK_STATUSES` 只含 dispatched/running，与 `excludeBusy` 注释及 `orchestrator.activeTasks()` 口径不符——协商探测窗口（本地 CLI 15s / remote 5min）内 HTTP/MCP `pod_dispatch` 并发可把第二个 worker 派到同一 per-slot worktree（写冲突/commit 链乱），正是 2026-09-03 slot 互斥要堵的洞 | 补齐 negotiating/accepted 两态，与 activeTasks() 同口径 |
| A2 | P1 | `processCompletion` 的 BUDGET_EXCEEDED 分支裸调 `missionMachine.pause()`：maxParallel≥2 时第二个在途 completion 撞预算 → InvalidTransitionError 逃进 handleCompletion，正常完成被误判 crash、报告丢弃（routeAndNegotiate 同场景早有 try/catch 守卫） | 同款 try/catch 并发收口守卫 |
| A3 | P1 | `task-machine.report()` 对 `need_clarify`/`blocked` 报告直接 `questions.join()`/`blockers.join()`——LLM 报告的 isReportLike 只校验 task_id/status，缺字段/非数组即 TypeError，提问语义整个炸成 crash 白烧重试 | Array.isArray 兜底空数组（信任边界 fail-closed） |
| A4 | P2 | plan 分支不看 `report.status`：planner 按契约输出 need_clarify（有 questions 无 plan）被按 plan 缺失判 silent_failure 硬失败，task_question 事件也不发（break 在 maybeEmitQuestion 之前） | status 先行裁决：need_clarify/blocked 走 report() 软/硬路径，done 才进提案裁决 |
| A5 | P2 | `maybeAutoReplan` guard 只认 escalated，而两个调用点都在 `rejectTerminal`（终态 rejected）之后——重规划调用全部死代码，能力缺口型拒绝从不自动重规划 | guard 扩到 rejected（plan 任务防自递归不变） |
| A6 | P2 | `summarize()` 只把「退避未到」的 blocked 算 waiting_backoff，退避已满 `shouldRetry=true` 的任务落进 tasksCompleted() 异常 → 误报 needs_human，而 maintenanceTick 30s 后明明会自动重驱（操作面与实际恢复行为矛盾） | retryable blocked 归入 waiting_backoff 口径 |
| A7 | P2 | `planSeq`/`replansUsed` 是实例字段：宿主重启后归零 → 重启后 replan 撞 DUPLICATE_TASK（错误逃逸成 completion_error）+ REPLAN_LIMIT 跨重启失效 | plan id 从存量任务重推导（max P-n +1）；计次改按 plan_replan_requested 事件数（任务创建成功才落盘，天然不占空预算） |
| A8 | P2 | `events.ts` 进度事件 id `ev-progress-${ts}-${seq}` 不含 slot_id，seq 又是 per-slot 计数——maxParallel≥2 同毫秒必撞 id（前端拿 id 当列表 key） | id 加入 slot_id |

## B. 存储与 worker 层（10 处）

| # | 严重度 | 缺陷 | 修复 |
|---|---|---|---|
| B1 | P1 | `memory.ts load()` 主文件缺失即返回 undefined，从不看 .bak——atomicWrite 的 main→bak 与 tmp→main 两次 rename 之间崩溃，磁盘上正是「main 缺失 .bak 完好」，整层记忆静默清零（store.ts 同窗口早有恢复） | 同 store.ts 处置：回读 .bak 并原子写回主文件自愈 |
| B2 | P1 | `claude-headless` 的 baselineHead 在 `exited` **之后**才读——恒等于结果 HEAD，`headSha !== baselineHead` 恒 false，commit_sha 校正分支整段死代码（模型虚报 sha ~19% 实证场景永不校正） | 捕获点移到 start() spawn 前（现有测试按调用序 mock，语义恰好因此变真） |
| B3 | P1 | `openPodData` 的 JSON 回退：损坏源自 JsonStore.open()（迁移路径）时主文件已被改名 .corrupt-*，回退再开 = 静默空库（且 pod.db 半建成后下次不再迁移）——违背「绝不静默开空库」 | STORE_CORRUPT fail-fast 重抛（plugin.apply 的 catch 兜住降级 503，R6/R10 仍成立）；rethrow 前先释放半开 SQLite 句柄 |
| B4 | P2 | SQLite 迁移判定只看 pod.db 文件存在：首启迁移中途崩溃留下已建表的库 + 未迁的 store.json → fresh=false 永久跳过迁移（静默空库） | 判定改 meta 标志 `json_migrated` + missions 行数；迁移插入全改 OR IGNORE 保证「tx 已提交 rename 未完成」窗口重入安全 |
| B5 | P2 | 三个 headless worker 的流式解析逐块 `toString('utf8')`：管道按字节切块，CJK 多字节字符跨块边界产出 U+FFFD → JSON.parse 静默失败（最终 result 行含中文报告时最致命） | 全部改 StringDecoder + 'close' 时残尾冲刷；claude 侧另加 `closed` promise（exit 可先于最后一批 stdout data——真实观察过的「偶发无报告退出」同征） |
| B6 | P2 | `Ledger.recordUsage` 账本与 mission 花费两次独立 persist，中间崩溃留下「账已记、花费未涨」永久分歧 → budgetStatus 低估、熔断晚触发 | PodStore 增可选 `recordUsageAtomic`（JsonStore 单次 persist / SqliteStore 单事务），未实现的测试桩回落两笔 |
| B7 | P2 | `pidAlive` 把 EPERM（进程活着但无权发信号）当死 → sweep 可删活进程在途 tmp，owner rename 时 ENOENT | ESRCH=死 / EPERM=活 |
| B8 | P2 | memory 开库清扫前缀 `'.memory.tmp-'` 永不匹配 tmpPathFor 产物 `memory.json.tmp-`——残骸只增不减（旧测试跟着错误字面量一起写错，已同步更正） | 前缀改 `${basename(filePath)}.tmp-` |
| B9 | P2 | `RemoteBackend.pollUntilDone` 卫星消失后无限重试且 onExit 永不触发——任务槽永久挂死，每次丢任务泄漏一个不死轮询循环；kill 依赖卫星回执 | 连续 60 次失败合成 failed completion 收口；kill 先落本地标记再发请求 |
| B10 | P2 | `ClaudeBackendOptions.registry` 被声明但从未读取——方案书 3.2 进程治理（pid↔slot↔task 唯一映射）实际是静默 no-op | 接线：spawn 注册、完成注销（DUPLICATE_PID 不阻断派发） |

## C. 协议与 HTTP 路由层（7 处）

| # | 严重度 | 缺陷 | 修复 |
|---|---|---|---|
| C1 | P1 | `/dispatch`、`/abort` 无方法检查：dispatch 无 body、abort 空 body 走默认 reason——跨站 `<img src=…>` 即可静默触发真实派发（烧钱）或中止 mission（loopback + 无 Origin 的图片请求可过全部现有防线） | 补 POST 405（与 /pause /resume 同口径） |
| C2 | P1 | `/a2a` 统一端点（Agent Card 主 URL）把 `req` 读两次：第二次拿到空流 → 所有 message/send / stream 一律 422，主入口形同虚设（只有 additionalInterfaces 旧入口能用） | 已解析 body 透传 handleA2aSend（新增 opts.body 参数）。**实测修复前 422、修复后 message/send → launch → Task 快照全通** |
| C3 | P1 | `matchApprovalId` 用 `/a-\d+/`：生产审批 id 是 `A-<毫秒>-<随机>`，第一个连字符截断 → IM 里「批准 A-…」永远 approval not found（IM 审批链路对生产 id 形同虚设） | 正则补可选第二段 `/a-\d+(?:-\d+)?/`，兼容旧短 id |
| C4 | P2 | 审批 merge 进行中（git + 锁重试可达数秒）mission 被并发 pause/abort（awaiting_approval 允许两者）→ completeAfterMerge 抛 InvalidTransitionError → 500，但合并已写进主树（abort 场景尤其违背预期） | 捕获 INVALID_TRANSITION 落 `merge_state_divergence` 一致性事件，按合并结果如实返回 |
| C5 | P2 | 双击/IM+UI 并发 approve → ApprovalConflictError 未映射 → 500 而非 409（/deny 同场景早映射 409，契约不一致；MCP pod_approve 同病） | pod-service.approve 把 APPROVAL_CONFLICT/INVALID_TRANSITION 转结构化 `{ok:false,conflict:true}`，三个消费面（HTTP/MCP/IM）统一收口 |
| C6 | P2 | IM 出站 `send()` 失败时重放标记不释放 → vendor 重试被「duplicate event」拒收，指令已执行但用户永远看不到回复 | send 失败释放 replayGuard 再抛（重试的重复执行由审批冲突/状态机守卫幂等拒绝） |
| C7 | P2 | standalone `/mcp` 分支 `void mcp.handle(req,res)` 无 .catch：initialize 的 server.connect、DELETE 的 server.close 在内部 try 外 reject → unhandledRejection 终止整个 standalone 进程 | 补 .catch 500 兜底（与其他分支同款） |
| C9 | P2 | `CronScheduler.history` 只增不减：1s 间隔 job 长驻进程每天 ~8.6 万条（慢性内存泄漏） | 上限 500 条（HISTORY_MAX），historyTail 语义不变 |

**未修（记录）**：A2A push webhook 的 SSRF 残余（只校验 scheme，loopback/link-local 目标放行）——代码注释已声明 allowlist 属部署侧职责，维持该设计决策不改。

## D. 端到端实测中发现的额外缺陷（第 26 处，已修）

| # | 缺陷 | 修复 |
|---|---|---|
| C8' | `A2A_DEFAULT_SLOTS` 能力词汇与规划器提案契约脱节：默认名册无「规划」（plan 任务派发即 escalate）、S-1 无「编码」（planner 标准产出盖不住）——**默认 A2A 启动 100% 转人工**，实测复现 | S-1 改 `['编码','规划','测试']`、S-2 保持 `['审查','文档']`。实测修复后 A2A message/send → 规划 → 实现 → 独立审查 → 审批 → 合并 → mission_done 全链路闭环 |

## E. 环境修复（仓库外）

- 系统 PATH（HKLM）损坏导致 npm/cmd 全体找不到 node：`MAVEN_HOME` 值带字面引号（`%MAVEN_HOME%\bin` 展开后引号混入 PATH，cmd 可执行搜索在此中断）、PATH 里 `%CATALINE_HOME%bin` 笔误（CATALINE→CATALINA + 缺 `\`）。已备份后修复注册表（备份：会话临时目录 env-backup/），新终端 cmd 下 node 解析恢复。

## F. 验证

- 单测：新增 `tests/audit-fixes-0905.test.ts` 24 例（每处修复对应至少一例）；全量 **943 passed / 1 skipped**。
- 门禁：`npm run verify` 全绿（typecheck 0 → coverage 门禁 → build）。
- HTTP 端到端（standalone `--demo` 模式，零 LLM 成本、真实 git/审批/状态机）：
  `launch → 规划(P-1) → 实现 T-1（need_clarify 提问 → 重派 → done）→ 独立审查 T-2/T-3 →
  审批卡 → approve → merge commit → mission_done`；事件链、账本、A2A Agent Card、
  GET /dispatch 405（C1）、/a2a message/send（C2）逐项实测通过。
  （真实 LLM 链 demo-chain.mjs 所需的本地代理 127.0.0.1:15721 当前未运行，留待凭据在线时复跑。）
